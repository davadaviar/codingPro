let htmlPreview1 = CodeMirror.fromTextArea(document.getElementById('html-preview-1'), {
    mode: 'xml',
    lineNumbers: true,
    tabSize: 4,
    theme: 'dracula',
    autoCloseTags: true,
});

let htmlPreview2 = CodeMirror.fromTextArea(document.getElementById('html-preview-2'), {
    mode: 'xml',
    lineNumbers: true,
    tabSize: 4,
    theme: 'dracula',
    autoCloseTags: true,
});

let htmlPreview3 = CodeMirror.fromTextArea(document.getElementById('html-preview-3'), {
    mode: 'xml',
    lineNumbers: true,
    tabSize: 4,
    theme: 'dracula',
    autoCloseTags: true,
});

let htmlPreview4 = CodeMirror.fromTextArea(document.getElementById('html-preview-4'), {
    mode: 'xml',
    lineNumbers: true,
    tabSize: 4,
    theme: 'dracula',
    autoCloseTags: true,
});

let htmlPreview5 = CodeMirror.fromTextArea(document.getElementById('html-preview-5'), {
    mode: 'xml',
    lineNumbers: true,
    tabSize: 4,
    theme: 'dracula',
    autoCloseTags: true,
});

let htmlPreview6 = CodeMirror.fromTextArea(document.getElementById('html-preview-6'), {
    mode: 'xml',
    lineNumbers: true,
    tabSize: 4,
    theme: 'dracula',
    autoCloseTags: true,
});